# Used by `npm run dev` in vite.config.ts to proxy /api and /ws during hot reload.
# Leave unset to default to the local docker-compose backend at http://localhost:5000.
VITE_BACKEND_URL=http://localhost:5000
