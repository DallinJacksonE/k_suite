import { ClientShell } from '../components/layout/ClientShell'

export function MarketsView() {
  return (
    <ClientShell>
      <section className="page-card">
        <p className="eyebrow">Markets</p>
        <h1>Market calendar coming soon.</h1>
        <p>This page will show upcoming events and details once the market calendar API is wired.</p>
      </section>
    </ClientShell>
  )
}
